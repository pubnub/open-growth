opengrowth.signals['*'] = ( customer, signal ) => {
    // customer param is automatically
    // augmented with clearbit and monkeylearn ☕️ 
    // console.log( signal, customer );// Run any.js for '*'
    // can be executed with opengrowth.signals['*']( customer, signal );
};
